% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/drop_staten_island.R
\name{drop_staten_island}
\alias{drop_staten_island}
\title{Drop Staten Island from a map}
\usage{
drop_staten_island(x, buffer = 10)
}
\arguments{
\item{x}{An \code{sf} object to crop. Must have a defined CRS.}

\item{buffer}{Numeric scalar giving the offset, in the map units of
\code{x}'s CRS, used to place the cut "just beyond" the Staten Island
and Breezy Point extremes. Defaults to \code{10} (feet when \code{x} uses
EPSG:2263, the default for maps in this package).}
}
\value{
An \code{sf} object of the same class as \code{x}, cropped to exclude
Staten Island.
}
\description{
Crop an \code{sf} object so that Staten Island is removed. The map is
first clipped on the west, just beyond the easternmost point of the
Staten Island polygon, and then clipped on the south, just below the
southernmost tip of Breezy Point (the southern tip of Queens).
}
\examples{
drop_staten_island(nyc_boros_sf)
}
